# 정적 파일 (이미지·PDF)

이 폴더에 프로필 사진, 로고, 배너, CV·학술이력서 PDF 등을 둡니다.

- **사이트**: `/content/files/파일명` 으로 제공됩니다.
- **옵시디언**: `public/content` 를 루트로 열면, 스크랩 md에서 `../files/이미지.png` 로 참조 시 미리보기가 됩니다.

이전에 `public/files/` 에 두었던 파일이 있다면 이 폴더(`public/content/files/`)로 옮겨 주세요.
